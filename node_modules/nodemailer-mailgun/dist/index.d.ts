import { Transport, SendMailOptions } from 'nodemailer';
import { Attachment } from 'nodemailer/lib/mailer';
export interface MailgunTransportOptions {
    url?: string;
    host?: string;
    protocol?: 'http:' | 'https:';
    port?: number;
    auth: {
        api_key: string;
        domain: string;
    } | {
        apiKey: string;
        domain: string;
    };
    timeout?: number;
}
export declare type MailgunTransportSendMailOptions = SendMailOptions & {
    attachments?: MailAttachment;
};
declare type MailAttachment = Attachment & {
    knownLength?: number;
};
declare const transport: (options: MailgunTransportOptions) => Transport;
export default transport;
